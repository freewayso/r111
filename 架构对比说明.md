# Sol2 架构设计：数据驱动 vs C++ 中心

## 当前架构问题 ❌

```cpp
// C++ 端（game_engine.hpp）
class GameEngine {
private:
    int player_health = 100;  // ❌ 数据在 C++ 端，无法热更新
    
public:
    void damagePlayer(int amount);  // ❌ 逻辑在 C++ 端
    int getPlayerHealth() const;
};
```

```lua
-- Lua 端（demo.lua）
function on_update()
    -- ❌ 只能调用 C++ 函数，数据不在这里
    local health = engine:get_player_health()
    engine:damage_player(10)
end
```

**问题：**
1. 修改游戏数据需要重新编译 C++
2. 无法通过热更新调整游戏参数
3. Lua 只是"调用者"，不是"拥有者"
4. 不够灵活，不利于快速迭代

## 推荐架构 ✅（数据驱动）

### 原则：
- **C++ = 引擎/系统层**（渲染、物理、输入、文件IO等）
- **Lua = 游戏逻辑/数据层**（游戏规则、数据、AI等）

```cpp
// C++ 端（game_engine.hpp）
class GameEngine {
private:
    sol::state lua;  // ✅ Lua 拥有数据
    
public:
    // ✅ 只提供系统级功能
    void logMessage(const std::string& msg);
    void playSound(const std::string& name);
    void renderSprite(int x, int y, const std::string& sprite);
};
```

```lua
-- Lua 端（demo.lua）
-- ✅ 数据在 Lua 端，可以热更新
GameData = GameData or {
    player = {
        health = 100,
        max_health = 100,
        mana = 50,
        level = 1
    },
    enemies = {},
    config = {
        damage_multiplier = 1.0,
        spawn_rate = 2.0
    }
}

-- ✅ 逻辑也在 Lua 端
function damagePlayer(amount)
    local real_damage = amount * GameData.config.damage_multiplier
    GameData.player.health = GameData.player.health - real_damage
    
    if GameData.player.health <= 0 then
        onPlayerDeath()
    end
end
```

**优势：**
1. ✅ 修改游戏数据/逻辑无需重新编译
2. ✅ 所有参数可以热更新
3. ✅ Lua 是数据的"拥有者"
4. ✅ 快速迭代，立即看到效果

