# 常见问题解决

## 1. "Lua function 'on_init' not found"

**原因：** 脚本文件没有被复制到输出目录

**解决方法：**

```powershell
# 手动复制脚本文件到 Debug 目录
Copy-Item "scripts" -Destination "build_vs2022\Debug\" -Recurse -Force

# 或者复制到 Release 目录
Copy-Item "scripts" -Destination "build_vs2022\Release\" -Recurse -Force
```

**或者重新编译项目：**

```powershell
cmake --build build_vs2022 --config Debug --target sol2_demo
```

CMake 会自动执行脚本复制（如果配置正确）。

## 2. "无法打开 .exe 进行写入"

**原因：** 程序正在运行

**解决方法：**

```powershell
# 方法 1: 强制关闭
taskkill /F /IM sol2_demo.exe

# 方法 2: 在程序窗口中按 Q 键退出

# 方法 3: 在 Visual Studio 中按 Shift+F5 停止调试
```

## 3. "找不到 lua54.dll"

**原因：** DLL 文件没有复制到输出目录

**解决方法：**

```powershell
# 复制 DLL 到 Debug 目录
Copy-Item "lua\lib\lua54.dll" -Destination "build_vs2022\Debug\" -Force

# 复制 DLL 到 Release 目录
Copy-Item "lua\lib\lua54.dll" -Destination "build_vs2022\Release\" -Force
```

## 4. Protobuf 相关错误

**症状：** `engine.serialize_player_pb` 为 nil

**原因：** Protobuf 未安装或未正确配置

**解决方法：**

```powershell
# 1. 安装 Protobuf
vcpkg install protobuf:x64-windows

# 2. 重新生成项目（使用 toolchain file）
cmake -S . -B build_vs2022 -G "Visual Studio 17 2022" -A x64 `
  -DCMAKE_TOOLCHAIN_FILE=[vcpkg路径]/scripts/buildsystems/vcpkg.cmake

# 3. 重新编译
cmake --build build_vs2022 --config Debug
```

## 5. "脚本加载失败" 或 "文件不存在"

**检查路径：**

```lua
-- 在 Lua 中检查
print("Current directory: " .. io.popen("cd"):read())

-- 检查文件是否存在
local file = io.open("scripts/demo.lua", "r")
if file then
    print("File found!")
    file:close()
else
    print("File NOT found!")
end
```

**确保工作目录正确：**

运行程序时应该在 `build_vs2022\Debug\` 目录，或者确保 `scripts/` 相对路径正确。

## 6. 热更新不工作

**检查：**

1. 确认已按 `A` 键启用自动重载
2. 确认修改了正确的文件（源文件，而不是复制的文件）
3. 确认保存了文件

**手动触发：**

按 `R` 键手动重载脚本

## 7. Visual Studio 编译错误

**常见错误：**

### C4819: 文件包含不能在当前代码页中表示的字符

**解决：** 不影响运行，可以忽略

或者在文件开头添加 UTF-8 BOM

### LNK2019: 未解析的外部符号

**可能原因：**
- Lua 库未正确链接
- Protobuf 库未正确链接

**检查：**
```cmake
# CMakeLists.txt 中确保有
target_link_libraries(sol2_demo ${LUA_LIBRARIES})
target_link_libraries(sol2_demo ${Protobuf_LIBRARIES})
```

## 快速修复脚本

创建一个 `fix_all.ps1`：

```powershell
# fix_all.ps1 - 一键修复常见问题

Write-Host "Fixing common issues..." -ForegroundColor Cyan

# 1. 关闭运行中的程序
taskkill /F /IM sol2_demo.exe 2>$null

# 2. 创建必要的目录
New-Item -ItemType Directory -Path "build_vs2022\Debug" -Force | Out-Null
New-Item -ItemType Directory -Path "build_vs2022\Release" -Force | Out-Null

# 3. 复制脚本文件
Copy-Item "scripts" -Destination "build_vs2022\Debug\" -Recurse -Force
Copy-Item "scripts" -Destination "build_vs2022\Release\" -Recurse -Force

# 4. 复制 DLL 文件
if (Test-Path "lua\lib\lua54.dll") {
    Copy-Item "lua\lib\lua54.dll" -Destination "build_vs2022\Debug\" -Force
    Copy-Item "lua\lib\lua54.dll" -Destination "build_vs2022\Release\" -Force
}

Write-Host "All fixed!" -ForegroundColor Green
Write-Host "You can now run: .\build_vs2022\Debug\sol2_demo.exe"
```

**使用方法：**

```powershell
.\fix_all.ps1
```

## 调试技巧

### 1. 启用详细日志

在 Lua 脚本中：

```lua
function on_init()
    engine:log("=== DEBUG: on_init called ===")
    engine:log("Script loaded from: " .. debug.getinfo(1).source)
end
```

### 2. 检查 Lua 状态

```lua
function on_update()
    -- 打印当前 Lua 版本
    engine:log("Lua version: " .. _VERSION)
    
    -- 打印全局变量
    for k, v in pairs(_G) do
        if type(v) == "function" then
            engine:log("Function: " .. k)
        end
    end
end
```

### 3. 捕获错误

```lua
function safe_call(func_name, ...)
    local func = _G[func_name]
    if not func then
        engine:log_error("Function not found: " .. func_name)
        return nil
    end
    
    local success, result = pcall(func, ...)
    if not success then
        engine:log_error("Error calling " .. func_name .. ": " .. tostring(result))
        return nil
    end
    
    return result
end
```

## 获取帮助

如果问题仍然存在：

1. 检查 `saves/` 目录是否可写
2. 查看控制台输出的错误信息
3. 确认所有依赖库已正确安装
4. 重新运行 CMake 配置

## 完整的重建流程

如果一切都不工作，完整重建：

```powershell
# 1. 清理
Remove-Item build_vs2022 -Recurse -Force -ErrorAction SilentlyContinue

# 2. 重新生成
cmake -S . -B build_vs2022 -G "Visual Studio 17 2022" -A x64

# 3. 编译
cmake --build build_vs2022 --config Debug

# 4. 复制文件
Copy-Item "scripts" -Destination "build_vs2022\Debug\" -Recurse -Force
Copy-Item "lua\lib\lua54.dll" -Destination "build_vs2022\Debug\" -Force

# 5. 运行
.\build_vs2022\Debug\sol2_demo.exe
```

现在应该一切正常了！🎉

