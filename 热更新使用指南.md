# Sol2 热更新（Hot Reload）使用指南

## 什么是热更新？

热更新（Hot Reload）允许您在程序运行时修改 Lua 脚本，无需重新编译或重启程序，修改后的代码会立即生效。这在游戏开发和快速迭代中非常有用。

## 功能特性

### ✅ 已实现的功能

1. **手动热重载** - 按 `R` 键手动触发脚本重载
2. **自动热重载** - 按 `A` 键启用/禁用自动检测文件修改
3. **文件监控** - 自动检测脚本文件的修改时间
4. **重载回调** - Lua 脚本可以定义 `on_reload()` 函数，在重载时执行
5. **错误处理** - 重载失败不会影响程序运行，会保持旧版本代码

## 使用方法

### 1. 运行程序

```bash
# 编译项目
cmake --build build_vs2022 --config Debug

# 运行程序
.\build_vs2022\Debug\sol2_demo.exe
```

或者在 Visual Studio 中按 F5 运行。

### 2. 交互式控制

程序运行后，您可以使用以下按键：

| 按键 | 功能 |
|------|------|
| `R` | 手动重载脚本 |
| `A` | 切换自动重载模式 |
| `Q` | 退出程序 |

### 3. 实时修改脚本

#### 方法 A：手动触发
1. 运行程序
2. 用编辑器打开 `scripts/demo.lua`
3. 修改代码（例如修改输出消息）
4. 保存文件
5. 在程序窗口中按 `R` 键
6. 立即看到修改效果！

#### 方法 B：自动检测
1. 运行程序
2. 按 `A` 键启用自动重载
3. 修改并保存 `scripts/demo.lua`
4. 程序会在下一帧自动检测并重载
5. 无需手动触发！

## 示例演示

### 示例 1：修改输出消息

**原始代码 (demo.lua):**
```lua
function on_update()
    game_time = game_time + 1
    engine:log_message("Game time: " .. game_time)
end
```

**修改为：**
```lua
function on_update()
    game_time = game_time + 1
    engine:log_message("⏰ 游戏时间: " .. game_time .. " 秒")
    engine:log_message("当前帧率: 0.5 FPS")
end
```

按 `R` 或等待自动重载，立即看到新的输出！

### 示例 2：添加新功能

**在 demo.lua 中添加：**
```lua
function on_reload()
    reload_count = reload_count + 1
    engine:log_message("🔥 脚本已重载 " .. reload_count .. " 次！")
    
    -- 可以在这里添加重载后的初始化逻辑
    engine:log_message("新功能已加载！")
end

-- 添加新的游戏逻辑
function check_player_status()
    local health = engine:get_player_health()
    if health < 30 then
        engine:log_message("⚠️ 警告：生命值过低！")
    elseif health > 80 then
        engine:log_message("✅ 状态良好")
    end
end
```

### 示例 3：动态调整游戏参数

```lua
-- 全局配置（可以热更新）
CONFIG = {
    damage_multiplier = 1.5,  -- 修改这个值可以立即改变伤害
    enemy_spawn_rate = 2.0,   -- 修改敌人生成速率
    debug_mode = true         -- 开关调试信息
}

function on_update()
    game_time = game_time + 1
    
    if CONFIG.debug_mode then
        engine:log_message("Debug: Frame " .. game_time)
        engine:log_message("Damage Multiplier: " .. CONFIG.damage_multiplier)
    end
end
```

修改 `CONFIG` 中的值并保存，立即生效！

## 代码 API

### C++ 端

```cpp
// GameEngine.hpp
class GameEngine {
public:
    // 重载当前脚本
    void reloadScript();
    
    // 检查并重载（如果文件被修改）
    void checkAndReload();
    
    // 启用/禁用自动重载
    void enableAutoReload(bool enable = true);
    
    // 检查脚本是否被修改
    bool isScriptModified() const;
};
```

**使用示例：**
```cpp
GameEngine engine;
engine.loadScript("script.lua");

// 启用自动重载
engine.enableAutoReload(true);

// 游戏循环
while (running) {
    engine.update();  // 会自动检查并重载
}

// 或手动重载
engine.reloadScript();
```

### Lua 端

在 Lua 脚本中定义回调函数：

```lua
-- 脚本加载时调用（第一次）
function on_init()
    print("Script initialized")
end

-- 热重载时调用
function on_reload()
    print("Script reloaded!")
    -- 在这里重新初始化需要的数据
end

-- 每帧调用
function on_update()
    -- 游戏逻辑
end

-- 程序退出时调用
function on_cleanup()
    print("Cleanup")
end
```

## 热更新最佳实践

### ✅ 推荐做法

1. **使用全局配置表**
   ```lua
   CONFIG = CONFIG or {
       speed = 100,
       damage = 20
   }
   ```
   使用 `or` 操作符保留重载前的值

2. **保护关键数据**
   ```lua
   player_score = player_score or 0  -- 重载时保留分数
   ```

3. **在 on_reload 中重新注册事件**
   ```lua
   function on_reload()
       register_all_callbacks()
       reinit_systems()
   end
   ```

4. **使用模块化设计**
   ```lua
   -- modules/player.lua
   local Player = {}
   function Player.new()
       return { health = 100 }
   end
   return Player
   ```

### ❌ 避免的问题

1. **不要依赖重载顺序**
   ```lua
   -- 不好
   x = y + 1  -- 如果 y 在后面定义会出错
   
   -- 好
   function init()
       x = y + 1
   end
   ```

2. **小心循环引用**
   ```lua
   -- 可能导致内存泄漏
   table1.ref = table2
   table2.ref = table1
   ```

3. **避免在全局作用域执行副作用代码**
   ```lua
   -- 不好：每次重载都会执行
   print("Script loaded")
   
   -- 好：在函数中执行
   function on_init()
       print("Script loaded")
   end
   ```

## 性能考虑

- 文件监控开销很小（只检查修改时间）
- 建议开发时启用自动重载，发布时禁用
- 大型脚本重载可能需要几毫秒（通常 < 10ms）

## 调试技巧

### 1. 打印重载信息
```lua
function on_reload()
    print("=== Reloaded at " .. os.date("%H:%M:%S") .. " ===")
    print("Version: 1.2.3")
end
```

### 2. 版本号管理
```lua
SCRIPT_VERSION = "1.0.0"

function on_reload()
    print("Script version: " .. SCRIPT_VERSION)
end
```

### 3. 错误恢复
```lua
function on_reload()
    local ok, err = pcall(function()
        -- 重新初始化
        init_systems()
    end)
    
    if not ok then
        print("Reload init failed: " .. err)
    end
end
```

## 常见问题

### Q: 修改脚本后没有效果？
A: 检查：
1. 文件是否正确保存
2. 是否启用了自动重载（按 A）
3. 是否按了 R 键手动重载
4. 检查控制台是否有错误信息

### Q: 重载后变量丢失了？
A: 使用 `or` 操作符保留值：
```lua
my_data = my_data or { score = 0 }
```

### Q: 如何在重载时保留对象？
A: 在 Lua 中，顶层定义的变量会保留：
```lua
-- 全局变量，重载时保留
player = player or create_player()

-- 局部变量，重载时会重置
local temp = 123  -- 每次重载都是新的
```

### Q: 可以热更新 C++ 代码吗？
A: 不可以。只能热更新 Lua 脚本。C++ 代码需要重新编译。
   这就是为什么建议把游戏逻辑放在 Lua 中！

## 进阶功能

### 多文件热更新

可以扩展系统支持多个脚本文件：

```cpp
// 监控多个文件
std::vector<std::string> scripts = {
    "scripts/player.lua",
    "scripts/enemy.lua",
    "scripts/world.lua"
};

for (const auto& script : scripts) {
    engine.loadScript(script);
}
```

### 增量更新

只重载被修改的模块：

```lua
-- 使用 require 加载模块
local Player = require("modules.player")

-- 重载时只更新特定模块
function on_reload()
    package.loaded["modules.player"] = nil
    Player = require("modules.player")
end
```

## 总结

热更新是 sol2 和 Lua 的强大特性，可以：

- ✅ 大幅提升开发效率
- ✅ 无需重启程序即可测试修改
- ✅ 适合快速迭代和原型开发
- ✅ 可以在运行时调整游戏参数

立即试试修改 `scripts/demo.lua` 并观察实时效果吧！🚀

