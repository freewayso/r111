# Sol2 Demo - Visual Studio 2022 快速启动指南

## 当前状态 ✅

您的项目已经完成以下配置：

1. ✅ Lua 库文件已下载并配置 (`lua/lib/lua54.lib`, `lua54.dll`)
2. ✅ Lua 头文件已配置 (`lua/include/*.h`)
3. ✅ CMakeLists.txt 已修改为支持 Windows
4. ✅ 自动复制 DLL 的配置已添加

## 立即开始使用（3 步）

### 第 1 步：在 Visual Studio 中打开项目

```
打开文件：F:\sol2_demo\sol2_demo\build_vs2022\Sol2Demo.sln
```

如果项目已经打开，请关闭 Visual Studio 然后重新打开。

### 第 2 步：设置启动项目

**重要：这是解决 "ALL_BUILD" 错误的关键步骤！**

1. 在 Visual Studio 的 **解决方案资源管理器** 中
2. 找到 `sol2_demo` 项目（**不是** ALL_BUILD）
3. **右键点击** `sol2_demo`
4. 选择 **"设为启动项目"**

您会看到 `sol2_demo` 项目变成 **粗体**，这表示它已经是启动项目了。

### 第 3 步：编译和运行

1. 选择配置：**Debug** 或 **Release**（顶部工具栏）
2. 按 **F5** 或点击 **"本地 Windows 调试器"** 按钮
3. 程序会自动：
   - 编译代码
   - 复制 `lua54.dll` 到输出目录
   - 复制 `scripts/` 文件夹到输出目录
   - 运行程序

## 常见问题解决

### 问题 1：找不到 lua54.dll

**症状：** 运行时提示 "由于找不到 lua54.dll，无法继续执行代码"

**解决方法：**

方法 A（推荐）：重新编译项目
```
1. 在 Visual Studio 中
2. 右键点击 sol2_demo 项目
3. 选择 "清理"
4. 然后选择 "生成" 或直接按 F7
```

方法 B：手动复制 DLL
```powershell
# 在项目根目录运行
Copy-Item lua\lib\lua54.dll -Destination build_vs2022\Debug\
Copy-Item lua\lib\lua54.dll -Destination build_vs2022\x64\Debug\
```

### 问题 2：无法启动程序 "ALL_BUILD"

**症状：** 错误提示 "无法启动程序 F:\sol2_demo\sol2_demo\build_vs2022\x64\Debug\ALL_BUILD"

**原因：** ALL_BUILD 不是可执行程序，它只是一个构建目标

**解决方法：**
1. 在解决方案资源管理器中
2. 右键点击 `sol2_demo` 项目
3. 选择 **"设为启动项目"**

### 问题 3：找不到 lua.h

**症状：** 编译错误 "fatal error C1083: 无法打开包括文件: 'lua.h'"

**解决方法：**
```powershell
# 验证头文件是否存在
Get-ChildItem lua\include

# 如果为空，重新复制
Copy-Item C:\Users\Administrator\Downloads\lua-5.4.2_Win64_dllw6_lib\include\* -Destination lua\include\
```

### 问题 4：链接错误 LNK2019

**症状：** 链接器错误，未解析的外部符号

**可能原因：**
- lua54.lib 文件不正确
- 使用了错误的 Lua 版本

**解决方法：**
```powershell
# 验证 lib 文件
Get-ChildItem lua\lib\lua54.lib

# 如果文件很小（< 10KB），可能需要重新生成
# 运行以下命令重新生成 lib 文件
& "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\lib.exe" /def:F:\sol2_demo\sol2_demo\lua\lib\lua54.def /out:F:\sol2_demo\sol2_demo\lua\lib\lua54.lib /machine:x64
```

## 项目结构说明

```
F:\sol2_demo\sol2_demo\
├── build_vs2022\              # CMake 生成的 VS2022 项目
│   ├── Sol2Demo.sln          # Visual Studio 解决方案文件
│   ├── Debug\                # Debug 编译输出
│   │   ├── sol2_demo.exe     # 可执行文件
│   │   ├── lua54.dll         # 自动复制的 DLL
│   │   └── scripts\          # 自动复制的脚本
│   └── Release\              # Release 编译输出
├── lua\                       # Lua 库文件
│   ├── include\              # Lua 头文件
│   │   ├── lua.h
│   │   ├── luaconf.h
│   │   ├── lualib.h
│   │   └── lauxlib.h
│   └── lib\                  # Lua 库文件
│       ├── lua54.lib         # Visual Studio 导入库
│       ├── lua54.dll         # Lua 运行时 DLL
│       └── lua54.def         # DLL 导出定义
├── sol\                       # sol2 头文件库
├── src\                       # 源代码
│   ├── main.cpp
│   ├── game_engine.cpp
│   └── game_engine.hpp
├── scripts\                   # Lua 脚本
│   └── demo.lua
└── CMakeLists.txt            # CMake 配置文件
```

## 验证配置

运行以下 PowerShell 命令验证所有文件都已正确配置：

```powershell
# 在项目根目录 F:\sol2_demo\sol2_demo 运行

Write-Host "检查 Lua 头文件..." -ForegroundColor Cyan
Get-ChildItem lua\include\*.h | Format-Table Name

Write-Host "`n检查 Lua 库文件..." -ForegroundColor Cyan
Get-ChildItem lua\lib\lua54.* | Format-Table Name, Length

Write-Host "`n检查 VS 项目文件..." -ForegroundColor Cyan
if (Test-Path "build_vs2022\Sol2Demo.sln") {
    Write-Host "✓ Sol2Demo.sln 存在" -ForegroundColor Green
} else {
    Write-Host "✗ Sol2Demo.sln 不存在" -ForegroundColor Red
}

Write-Host "`n检查源代码..." -ForegroundColor Cyan
Get-ChildItem src\*.cpp, src\*.hpp | Format-Table Name

Write-Host "`n检查脚本..." -ForegroundColor Cyan
Get-ChildItem scripts\*.lua | Format-Table Name
```

## 下一步

项目成功运行后，您会看到类似以下输出：

```
Starting Sol2 Demo...
Lua initialized successfully!

--- Update 1 ---
Calling Lua update()...

--- Update 2 ---
Calling Lua update()...
...
Demo completed!
```

## 编译选项

### Debug 模式（开发调试）
- 包含调试信息
- 未优化
- 可以设置断点
- 文件较大

### Release 模式（正式发布）
- 完全优化
- 无调试信息
- 性能最佳
- 文件较小

选择方式：在 Visual Studio 顶部工具栏的配置下拉框中选择。

## 使用 CMake GUI（可选）

如果需要重新配置项目：

1. 打开 **CMake GUI**
2. **Where is the source code:** `F:/sol2_demo/sol2_demo`
3. **Where to build the binaries:** `F:/sol2_demo/sol2_demo/build_vs2022`
4. 点击 **Configure**，选择 **Visual Studio 17 2022**
5. 点击 **Generate**
6. 点击 **Open Project** 打开 Visual Studio

## 需要帮助？

如果遇到其他问题：

1. 检查 `lua/include` 和 `lua/lib` 目录是否包含所有必要文件
2. 确保使用的是 64 位配置（x64）
3. 查看 Visual Studio 的 **输出** 窗口了解详细错误信息
4. 查看 **错误列表** 窗口了解编译/链接错误

---

**祝您编码愉快！** 🎉

